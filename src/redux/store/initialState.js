import { userInitialState as user } from './initialStates/userInitialState';

/**
 * An object that should hold all initialStates from the system
 * @author Samuel Rutakayile
 * @since 06.05.2020
 */
export const initialState = {
  user
};
