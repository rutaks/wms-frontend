import * as Yup from 'yup';
import { containerVolume } from './validators';

export const modifyReportInitialValues = {
  priority: null,
  status: null,
  assigneeUuid: null
};

// export const linkDeviceToClientValidationSchema = Yup.object().shape({

// });
